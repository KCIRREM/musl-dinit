diff --git a/Makefile b/Makefile
index 19b1b1b..761b5ac 100644
--- a/Makefile
+++ b/Makefile
@@ -48,10 +48,8 @@ SERVICES = \
 	tmpfiles-dev \
 	tmpfiles-setup \
 	tmpfs \
-	udevd \
-	udevd-early \
-	udev-settle \
-	udev-trigger
+	mdevd \
+	mdevd-coldplug
 
 TARGETS = \
 	early-console.target \
@@ -84,8 +82,6 @@ SCRIPTS = \
 	pseudofs \
 	rclocal \
 	tmpfiles \
-	udevd \
-	udev-trigger \
 	vconsole
 
 TTY_SERVICES = \
@@ -171,7 +167,7 @@ install:
 	done
 	# install default service
 	ln -sf ../agetty $(DESTDIR)$(DINITSRVDIR)/boot.d/agetty
-	ln -sf ../udevd $(DESTDIR)$(DINITSRVDIR)/boot.d/udevd
+	ln -sf ../mdevd $(DESTDIR)$(DINITSRVDIR)/boot.d/mdevd
 	# shutdown hook
 	install -Dm755 misc/shutdown-hook $(DESTDIR)$(LIBDIR)/dinit/shutdown-hook
 	# misc
diff --git a/services/early-devices.target b/services/early-devices.target
index b9713c4..be2df02 100644
--- a/services/early-devices.target
+++ b/services/early-devices.target
@@ -1,3 +1,3 @@
 type       = internal
-depends-on = udevd-early
-depends-ms = udev-settle
+depends-on = mdevd
+depends-ms = mdevd-coldplug
diff --git a/services/fsck-root b/services/fsck-root
index 2dafa29..7b3990f 100644
--- a/services/fsck-root
+++ b/services/fsck-root
@@ -4,5 +4,5 @@ restart       = false
 options       = starts-on-console start-interruptible skippable
 start-timeout = 0
 depends-on    = early-prepare.target
-depends-ms    = udevd-early
-after         = udev-settle
+depends-ms    = mdevd
+after         = mdevd-coldplug
diff --git a/services/hwclock b/services/hwclock
index a2e0151..92bf56d 100644
--- a/services/hwclock
+++ b/services/hwclock
@@ -2,6 +2,6 @@ type         = scripted
 command      = /usr/lib/dinit/hwclock start
 stop-command = /usr/lib/dinit/hwclock stop
 restart      = false
-depends-on   = udevd-early
+depends-on   = mdevd
 depends-on   = early-prepare.target
 depends-on   = early-root-rw.target
