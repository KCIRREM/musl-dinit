type            = scripted
command         = /usr/lib/dinit/apparmor start
stop-command    = /usr/lib/dinit/apparmor stop

depends-on      = early-fs-local.target
depends-on      = pseudofs
before          = local.target

start-timeout   = 120
logfile         = /var/log/dinit/apparmor.log
